/** @type {import('next').NextConfig} */
const config = {
  // experimental: {
  //   serverActions: true,
  // },
  images: {
    domains: ['cdn-demo.algolia.com'],
  },
};

module.exports = config;
